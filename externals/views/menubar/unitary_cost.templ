package menubar

templ UnitaryCost(active string) {
    <ul class="m-0 p-0 list-none text-sm">
        <li class="py-3 px-0 text-slate-300 text-xs uppercase font-bold tracking-wider"><span>Unitario Costo</span></li>
        <li>
            <a
                href="#"
                class={ "flex py-2 px-0 gap-3 item-center text-xs hover:text-slate-200 no-underline", templ.KV("text-slate-100", active == "balance"), templ.KV("text-slate-400", active != "balance") }
            >
                <span>Cantidades</span>
            </a>
        </li>
        <li>
            <a
                href="#"
                class={ "flex py-2 px-0 gap-3 item-center text-xs hover:text-slate-200 no-underline", templ.KV("text-slate-100", active == "balance"), templ.KV("text-slate-400", active != "balance") }
            >
                <span>Análisis</span>
            </a>
        </li>
    </ul>
}
